using System.Collections.Generic;
using UnityEngine;


public class Map_Viz : MonoBehaviour
{
    public  List<Transform> CountryTranformList; 
    // Start is called once before the first ecution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
