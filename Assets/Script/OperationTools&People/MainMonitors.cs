using UnityEngine;
using UnityEngine.UI;
using TMPro;
public class MainMonitors : MonoBehaviour
{
    public int totalThreatNum, totalOperation, totalContainment, TotalThreatMean, Operationrate, Containmentrate,Resouresusage,Maxresoures;
    public TextMeshProUGUI totalThreatText, totalOperationText, totalContainmentText, TotalThreatMeanText, OperationrateText, ContainmentrateText, ResouresusageText;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
